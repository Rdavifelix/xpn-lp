import { Stack } from 'expo-router';

// Placeholder root layout — replaced with providers (React Query, Auth, fonts,
// SafeArea) in the navigation-wiring phase.
export default function RootLayout() {
  return <Stack />;
}
